<form method="post" action="{{ path('quote_type_delete', {'id': quote_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ quote_type.id) }}">
    <button class="button is-danger"><i class="far fa-trash-alt"></i> Delete</button>
</form>
