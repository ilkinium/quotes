{% extends 'base.html.twig' %}

{% block title %}QuoteType{% endblock %}

{% block body %}
    <h1>QuoteType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ quote_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ quote_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('quote_type_index') }}">back to list</a>

    <a href="{{ path('quote_type_edit', {'id': quote_type.id}) }}">edit</a>

    {{ include('quote_type/_delete_form.html.twig') }}
{% endblock %}
