{% extends 'base.html.twig' %}

{% block title %}Edit Quotes{% endblock %}

{% block body %}
    <h1 class="title">Edit Quotes</h1>

    {{ include('quotes/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('quotes_index') }}">back to list</a>

    {{ include('quotes/_delete_form.html.twig') }}
{% endblock %}
